[[stack]]
name = "kanboard"
deploy = true

[stack.config]
server = "Local"
git_provider = "github.com"
repo = "blavogiez-org/proxmox-gitops"
branch = "main"
run_directory = "services/kanboard"
file_paths = ["docker-compose.yml"]
