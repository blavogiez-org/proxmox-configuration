[[stack]]
name = "kanboard"
deploy = true

[stack.config]
server = "Local"
git_provider = "github.com"
repo = "blavogiez-org/proxmox-configuration"
branch = "main"
clone_path = "/etc/komodo/stacks/kanboard"
run_directory = "/etc/komodo/stacks/kanboard/services/kanboard"
file_paths = ["docker-compose.yml"]
