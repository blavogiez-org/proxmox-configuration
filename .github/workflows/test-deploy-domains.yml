name: Appel d'un déploiement front (pour exemple)

on:
  push:
    branches: [main, 3*]
  workflow_dispatch:

permissions:
  contents: read
  
jobs:
  deploy:
    uses: jobacogiez-org/proxmox-gitops/.github/workflows/deploy-domains.yml@3-installation-vault
    secrets: inherit
