création du Template pour les VM (ici Debian)

cela va accélerer les installations des futures instances puisqu'on part d'une image extraite et on la modifie, plutôt que de créer à chaque fois
il s'agit du standard recommandé pour faire du Terraform dans Proxmox