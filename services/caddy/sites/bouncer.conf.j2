# tool pour voir son ip

# bouncer qui montre l'ip de la requête (sympa pour voir l'ip utilisée pour debug le vpn)
http://bouncer.priv.{{ domain }} {
    respond "IP de la requête : {remote_host}" 200
}

http://bouncer.{{ domain }} {
    respond "IP de la requête : {remote_host}" 200
}
