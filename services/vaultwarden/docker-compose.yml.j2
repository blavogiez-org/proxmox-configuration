services:
  vaultwarden:
    image: vaultwarden/server:1.36.0
    container_name: vaultwarden
    restart: unless-stopped
    environment:
      DOMAIN: "https://{{ domain }}"
    volumes:
      - ./vw-data/:/data/
    ports:
      - 8000:80